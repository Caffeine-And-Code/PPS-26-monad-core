package monad_core.simulator.presentation.helpers

import monad_core.simulator.presentation.resources.ImageConfigRecord

private[presentation] case class MockImageConfig() extends ImageConfigRecord("/")
